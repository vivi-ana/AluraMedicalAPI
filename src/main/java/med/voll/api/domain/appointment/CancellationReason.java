package med.voll.api.domain.appointment;

/**
 * Enum representing the reasons for appointment cancellation.
 */
public enum CancellationReason {
    PATIENT_CANCELLED,
    DOCTOR_CANCELLED,
    OTHER;
}